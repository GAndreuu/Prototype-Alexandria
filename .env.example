# Variáveis de Ambiente - ASI Monolith V12
# Copie este arquivo para .env e preencha com suas credenciais reais

# === API Keys ===
GEMINI_API_KEY=your_gemini_api_key_here

# === Configurações de API ===
API_HOST=0.0.0.0
API_PORT=8000

# === Segurança ===
# Lista de APIs permitidas (separadas por vírgula)
ALLOWED_APIS=api.example.com,api.trusted.com

# Domínios bloqueados (separadas por vírgula)
BLOCKED_DOMAINS=malicious.com,spam.com

# === Logging ===
LOG_LEVEL=INFO
LOG_FILE=data/logs/asi.log

# === Performance ===
# Número de workers para processamento paralelo
MAX_WORKERS=4

# Tamanho do cache de embeddings
EMBEDDING_CACHE_SIZE=1000

# === Modelos ===
# Modelo de embeddings (sentence-transformers)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Modelo Gemini
GEMINI_MODEL=gemini-1.5-pro

# === Dados ===
# Diretório de dados
DATA_DIR=./data

# Arquivo de índice SFS
INDEX_FILE=./data/knowledge.sfs

# === Topologia ===
# Número de clusters
N_CLUSTERS=256

# Tamanho dos chunks de texto
CHUNK_SIZE=512

# === Causal Engine ===
# Mínimo de co-ocorrências para considerar causal
MIN_CO_OCCURRENCE=3

# Threshold para força de variável latente
LATENT_VARIABLE_THRESHOLD=0.7

# === Critic Agent ===
# Tolerância de risco (0.0 a 1.0)
RISK_TOLERANCE=0.7

# === Development ===
# Modo debug
DEBUG=False

# Reload automático
RELOAD=True
